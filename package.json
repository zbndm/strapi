{
  "name": "pages-plugins-example",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "2ll": "latest",
    "88b": "^1.1.6",
    "@cloudflare/pages-plugin-static-forms": "^1.0.0",
    "@cloudflare/workers-types": "^3.16.0"
  },
  "scripts": {
    "build": "npx wrangler pages functions build --outfile static/_worker.js && npx wrangler pages publish static",
    "start": "npx wrangler pages dev static"
  }
}
