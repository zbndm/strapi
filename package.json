{
  "name": "pages-plugins-example",
  "scripts": {
    "start": "wrangler pages functions build --outfile public/_worker.js && wrangler pages publish public",
    "build": "wrangler pages functions build --outfile public/_worker.js",
    "g": "git add . && git commit -m 0 && git push -u origin main -f"
  },
  "version": "1.0.0",
  "dependencies": {
    "2ll": "latest",
    "@cloudflare/pages-plugin-static-forms": "^1.0.0",
    "@cloudflare/workers-types": "^3.16.0"
  }
}
